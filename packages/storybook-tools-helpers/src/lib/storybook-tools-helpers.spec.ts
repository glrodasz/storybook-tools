import { storybookToolsHelpers } from './storybook-tools-helpers';

describe('storybookToolsHelpers', () => {
  it('should work', () => {
    expect(storybookToolsHelpers()).toEqual('storybook-tools-helpers');
  });
});
