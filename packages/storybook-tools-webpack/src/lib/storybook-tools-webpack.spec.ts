import { storybookToolsWebpack } from './storybook-tools-webpack.js';

describe('storybookToolsWebpack', () => {
  it('should work', () => {
    expect(storybookToolsWebpack()).toEqual('storybook-tools-webpack');
  });
});
